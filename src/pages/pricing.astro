---
import PageLayout from '../layouts/PageLayout.astro';
import { pricingPlans } from '../data/pricing';
import { getTranslation } from '../data/languages';
import PricingCalculatorWrapper from '../components/PricingCalculatorWrapper';

// Get user language from URL or default to English (simple approach)
const currentLang = Astro.url.pathname.split('/')[1] || 'en';
const t = getTranslation(currentLang);
---

<PageLayout 
  title="Pricing"
  description="Explore our flexible pricing plans designed to fit your needs, from free trials to enterprise solutions."
>
  <!-- Hero Section -->
  <section class="py-20 bg-gray-50 dark:bg-gray-800">
    <div class="container mx-auto px-6 text-center">
      <h1 class="text-4xl md:text-5xl font-bold text-gray-900 dark:text-white mb-6" data-i18n="pricingTitle">
        Simple, Transparent Pricing
      </h1>
      <p class="text-xl text-gray-600 dark:text-gray-300 max-w-3xl mx-auto" data-i18n="pricingSubtitle">
        Start free and scale as you grow. No hidden fees, no surprises.
      </p>
    </div>
  </section>

  <!-- Pricing Cards -->
  <section class="py-20 bg-white dark:bg-gray-900">
    <div class="container mx-auto px-6">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-6xl mx-auto">
        {pricingPlans.map((plan) => (
          <div class={`
            relative bg-white dark:bg-gray-800 rounded-2xl shadow-lg flex flex-col h-full
            ${plan.popular ? 'border-2 border-blue-500' : 'border border-gray-200 dark:border-gray-700 p-6'}
            ${plan.popular ? 'popular-card' : 'hover:shadow-xl'}
          `}>
            {plan.popular && (
              <div class="absolute -top-4 left-1/2 transform -translate-x-1/2 z-10">
                <span class="whitespace-nowrap bg-gradient-to-r from-blue-500 to-indigo-600 text-white px-4 py-2 rounded-full text-sm font-semibold" data-i18n="mostPopular">
                  Most Popular
                </span>
              </div>
            )}
            
            <div class={`${plan.popular ? 'bg-white dark:bg-gray-800 rounded-2xl p-6' : ''} flex flex-col h-full`}>
              <div class="text-center mb-8">
                <div class="text-4xl mb-2">{plan.icon}</div>
                <h3 class="text-2xl font-bold text-gray-900 dark:text-white mb-2" data-i18n={`${plan.id}Plan`}>
                  {plan.name}
                </h3>
                <p class="text-gray-600 dark:text-gray-300 mb-6" data-i18n={`${plan.id}Description`}>
                  {plan.description}
                </p>
                
                <div class="mb-6">
                  {plan.bandwidthPriceKey && (
                    <div class="text-green-500 dark:text-green-400">                      
                      <strong class="text-3xl mt-1">${plan.bandwidthPrice}</strong>
                    </div>
                    <div class="text-sm text-green-600 dark:text-green-400" data-i18n={plan.bandwidthPriceKey}></div>
                  )}
                  {plan.storagePriceKey && (
                    <div class="text-orange-600 dark:text-orange-400 mt-2">                      
                      <span class="text-2xl mx-1 font-bold">${plan.storagePrice}</span>
                      <small class="text-sm text-orange-600 dark:text-orange-400" data-i18n={plan.storagePriceKey}></small>
                    </div>                   
                  )}
                  {plan.id !== 'enterprise' ? 
                    <div class="text-sm text-gray-600 dark:text-gray-400 mb-2" data-i18n="noStorageFees">
                      No storage fees for this plan
                    </div> : ''
                  }
                  <div class="text-sm text-gray-600 dark:text-gray-400 mt-2" data-i18n={`${plan.id}AutoExtension`}>
                    {plan.autoExtension}
                  </div>
                  
                  {plan.id === 'trial' && (
                    <div class="text-sm text-blue-600 dark:text-blue-400 mt-2" data-i18n="trialBandwidthLimit">
                      {plan.bandwidth}
                    </div>
                  )}
                  <div class="mb-6 text-center">
                  
              </div>
                </div>
              </div>
              
              <ul class="space-y-4 mb-4 flex-grow">
                {plan.features.map((feature, index) => (
                  <li class="flex items-start">
                    <svg class="w-5 h-5 text-green-500 mt-0.5 mr-3 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                      <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                    </svg>
                    <span class="text-gray-700 dark:text-gray-300" data-i18n={`${plan.id}Feature${index + 1}`}>
                      {feature}
                    </span>
                  </li>
                ))}
              </ul>
              
              {plan.id === 'enterprise' && (
                <div class="mb-6 p-4 bg-orange-50 dark:bg-orange-900/20 border border-orange-200 dark:border-orange-800 rounded-lg">
                  <p class="text-sm text-orange-800 dark:text-orange-200" data-i18n="enterpriseNote">
                    {plan.specialNote}
                  </p>
                </div>
              )}
              
              {/* Calculator button for paid plans */}
              {plan.showCalculator && (
                <div class="mb-6">
                  <a 
                    href="#pricing-calculator"
                    class="block w-full text-center py-3 px-6 bg-gradient-to-r from-green-500 to-emerald-600 text-white rounded-lg font-semibold hover:from-green-600 hover:to-emerald-700 shadow-md hover:shadow-lg"
                    data-i18n="calculator"
                  >
                    Calculator
                  </a>
                </div>
              )}
              
              <a 
                href={plan.id !== 'trial' ? '/contact' : 'https://app.snapbyte.io/auth/register'}
                class={`
                  block w-full text-center py-4 px-6 rounded-lg font-semibold mt-auto
                  ${plan.popular 
                    ? 'bg-gradient-to-r from-blue-500 to-indigo-600 text-white hover:from-blue-600 hover:to-indigo-700 shadow-md hover:shadow-lg' 
                    : 'bg-gray-100 dark:bg-gray-700 text-gray-900 dark:text-white hover:bg-gray-200 dark:hover:bg-gray-600'
                  }
                `}
                data-i18n={plan.id === 'trial' ? 'getStartedFree' : 'contactSales'}
              >
                Contact Sales
              </a>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Pricing Calculator Section -->
  <section id="pricing-calculator" class="py-20">
    <PricingCalculatorWrapper client:load plans={pricingPlans} translations={t} />
  </section>
  
  <!-- FAQ Section -->
  <section class="py-20 bg-gray-50 dark:bg-gray-800">
    <div class="container mx-auto px-6">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 dark:text-white mb-4" data-i18n="faqTitle">
          Frequently Asked Questions
        </h2>
      </div>
      
      <div class="max-w-3xl mx-auto space-y-6">
        <div class="bg-white dark:bg-gray-700 rounded-lg p-6 shadow-sm">
          <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-2" data-i18n="faqQuestion1">
            Can I change plans anytime?
          </h3>
          <p class="text-gray-600 dark:text-gray-300" data-i18n="faqAnswer1">
            Yes, you can upgrade or downgrade your plan at any time. Changes take effect immediately, 
            and we'll prorate any billing adjustments.
          </p>
        </div>
        
        <div class="bg-white dark:bg-gray-700 rounded-lg p-6 shadow-sm">
          <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-2" data-i18n="faqQuestion3">
            Do you offer custom enterprise solutions?
          </h3>
          <p class="text-gray-600 dark:text-gray-300" data-i18n="faqAnswer3">
            Absolutely! Our Enterprise plan includes custom configurations, dedicated support, 
            and can be tailored to meet specific compliance and performance requirements.
          </p>
        </div>
        
        <div class="bg-white dark:bg-gray-700 rounded-lg p-6 shadow-sm">
          <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-2" data-i18n="faqQuestion4">
            Is there any storage limit or quota included in the plan?
          </h3>
          <p class="text-gray-600 dark:text-gray-300" data-i18n="faqAnswer4">
            With our Pro Plan, there is no storage limit or quota. You can cache as much data as needed. Please note: files that haven't been downloaded in the past 30 days will be automatically purged to optimize resources. However, storage is free and you can re-cache any file at any time with no limitations.
          </p>
        </div>
        
        <div class="bg-white dark:bg-gray-700 rounded-lg p-6 shadow-sm">
          <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-2" data-i18n="faqQuestion5">
            Are there any requirements or restrictions on the ratio between stored content and bandwidth usage?
          </h3>
          <p class="text-gray-600 dark:text-gray-300" data-i18n="faqAnswer5">
            No, there are no such restrictions. Snapbyte is a data caching system, and we only charge based on actual bandwidth usage. In fact, we encourage high download activity. For low-access files, we apply the 30-day auto-purge policy mentioned above, but you're always free to request those files be cached again at any time.
          </p>
        </div>
        
        <div class="bg-white dark:bg-gray-700 rounded-lg p-6 shadow-sm">
          <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-2" data-i18n="faqQuestion6">
            Do you have any usage guidelines or best practices for optimal caching efficiency?
          </h3>
          <div class="text-gray-600 dark:text-gray-300">
            <p class="mb-3" data-i18n="faqAnswer6">
              Snapbyte is still in its early stage, and while our documentation is evolving, you can find helpful information at: https://docs.snapbyte.io. We also offer free support and testing to help you get started. Feel free to reach out directly via Telegram to our co-founder: @neokrypt
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>
</PageLayout>

<style>
  /* Simplified hover effects - no animations */
  .popular-card {
    box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.3);
  }

  /* Simple hover states without transitions */
  .hover\:shadow-xl:hover {
    box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
  }

  .hover\:shadow-lg:hover {
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
  }

  /* Remove all transition animations */
  * {
    transition: none !important;
    animation: none !important;
  }

  /* Static styles for dark mode without transitions */
  :global(.dark) .popular-card {
    box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.4);
  }
</style>
